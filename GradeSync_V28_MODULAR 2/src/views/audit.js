import { col,getDocs,query,orderBy,limit } from '../data/firestore.js';
import { esc,fmtTime,$ } from '../utils/dom.js';
export async function renderAudit(){const view=$('#view');const snap=await getDocs(query(col('audit_logs'),orderBy('timestamp','desc'),limit(500)));const rows=snap.docs.map(d=>({id:d.id,...d.data()}));view.innerHTML=`<div class="toolbar"><div><h3 style="margin:0">Truy vết lịch sử</h3><div class="tiny muted">Hiển thị tối đa 500 bản ghi gần nhất.</div></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Thời gian</th><th>Loại</th><th>Người thao tác</th><th>Nhóm</th><th>Thí sinh</th><th>Chi tiết</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${esc(fmtTime(x.timestamp))}</td><td>${esc(x.type||'-')}</td><td>${esc(x.actorName||x.graderName||'-')}</td><td>${esc(x.groupName||x.groupId||'-')}</td><td>${esc(x.candidateName||x.candidateId||'-')}</td><td>${esc(x.changeContent||x.detail||'')}</td></tr>`).join('')}</tbody></table></div>`}
