import { store } from '../state/store.js';
import { esc,toast,$ } from '../utils/dom.js';
import { setQueueState,callNext } from '../services/queue.js';
import { ref,setDoc,serverTimestamp } from '../data/firestore.js';
export function renderQueue(){const st=store.get(),view=$('#view'),admin=['Admin','SuperAdmin'].includes(st.user.role),groups=admin?[...st.groups.values()]:(st.user.assignedGroups||[]).map(id=>st.groups.get(id)).filter(Boolean);if(!st.selectedGroupId&&groups[0])st.selectedGroupId=groups[0].id;const cands=[...st.candidates.values()].filter(c=>c.groupId===st.selectedGroupId).sort((a,b)=>{const aa=a.queueStatus==='grading'?1:0,bb=b.queueStatus==='grading'?1:0;if(aa!==bb)return bb-aa;return Number(a.queueOrder||999999)-Number(b.queueOrder||999999)});view.innerHTML=`<div class="toolbar"><select id="queue-group" class="select" style="max-width:360px">${groups.map(g=>`<option value="${g.id}" ${g.id===st.selectedGroupId?'selected':''}>${esc(g.name)}</option>`).join('')}</select>${admin?'<button class="btn primary" id="call-next">Mời thí sinh tiếp theo</button>':''}</div><div class="card">${cands.map((c,i)=>`<div class="queue-row ${c.queueStatus==='grading'?'current':''}"><strong>#${i+1}</strong><div><strong>${esc(c.name)}</strong><div class="tiny muted">${esc(c.code)}</div></div><div>${admin?`<label class="badge ${c.isPresent?'ok':'gray'}"><input type="checkbox" data-present="${c.id}" ${c.isPresent?'checked':''}> ${c.isPresent?'Có mặt':'Chưa điểm danh'}</label>`:`<span class="badge ${c.isPresent?'ok':'gray'}">${c.isPresent?'Có mặt':'Chưa điểm danh'}</span>`}</div>${admin?`<div><button class="btn small" data-q="waiting" data-id="${c.id}">Chờ</button> <button class="btn small ok" data-q="grading" data-id="${c.id}">Đang thi</button> <button class="btn small" data-q="done" data-id="${c.id}">Xong</button></div>`:`<span class="badge ${c.queueStatus==='grading'?'ok':'gray'}">${esc(c.queueStatus||'waiting')}</span>`}</div>`).join('')||'<div class="pad muted">Chưa có thí sinh.</div>'}</div>`;$('#queue-group').onchange=e=>{st.selectedGroupId=e.target.value;store.emit('selection')};if(admin){$('#call-next').onclick=async()=>{const c=await callNext(cands);toast(c?`Đã mời ${c.name}`:'Không còn thí sinh chờ',c?'ok':'error')};view.querySelectorAll('[data-q]').forEach(b=>b.onclick=async()=>setQueueState(st.candidates.get(b.dataset.id),b.dataset.q,Date.now()));view.querySelectorAll('[data-present]').forEach(x=>x.onchange=async()=>{const c=st.candidates.get(x.dataset.present);const isPresent=x.checked;await setDoc(ref('candidates',c.id),{isPresent,attendanceOrder:isPresent?Date.now():0,attendanceUpdatedAt:serverTimestamp()},{merge:true});toast(isPresent?`Đã điểm danh ${c.name}`:`Đã bỏ điểm danh ${c.name}`)})}}
