import { store } from '../state/store.js';
import { esc,toast,$,confirmModal } from '../utils/dom.js';
import { groupStatus } from '../utils/time.js';
import { saveScore,validateBeforeSubmit } from '../services/scoring.js';
import { ref,setDoc,serverTimestamp } from '../data/firestore.js';
import { audit } from '../services/audit.js';
import { normalizeContent,sectionAccess } from '../services/content.js';
const timers=new Map();const pendingDrafts=new Map();let contentSection='scores';let viewMode=localStorage.getItem('gs28GradingView')||'personal';
function userScore(st,cid){return st.scores.get(`${st.user.authUid}_${cid}`)||[...st.scores.values()].find(s=>s.candidateId===cid&&s.graderUid===st.user.authUid)||{}}
function groupCandidates(st,gid){return[...st.candidates.values()].filter(c=>c.groupId===gid).sort((a,b)=>{const ac=a.queueStatus==='grading'?1:0,bc=b.queueStatus==='grading'?1:0;if(ac!==bc)return bc-ac;return Number(a.queueOrder||999999)-Number(b.queueOrder||999999)})}
export function renderGrading(){const st=store.get(),view=$('#view'),user=st.user,admin=['Admin','SuperAdmin'].includes(user.role);const allowed=admin?[...st.groups.values()]:[...(user.assignedGroups||[])].map(id=>st.groups.get(id)).filter(Boolean);if(!st.selectedGroupId&&allowed[0])st.selectedGroupId=allowed[0].id;const g=st.groups.get(st.selectedGroupId),cfg=g?normalizeContent(g):null;const contentTabs=g?`<div class="tabs" style="margin-bottom:12px"><button data-section="scores" class="${contentSection==='scores'?'active':''}">Chấm điểm</button>${cfg?.scoringContent?.enabled?`<button data-section="scoring" class="${contentSection==='scoring'?'active':''}">Nội dung tiêu chí</button>`:''}${cfg?.interviewContent?.enabled?`<button data-section="interview" class="${contentSection==='interview'?'active':''}">Câu hỏi phỏng vấn</button>`:''}</div>`:'';view.innerHTML=`<div class="toolbar"><div class="left"><select id="grade-group" class="select" style="min-width:280px">${allowed.map(x=>`<option value="${x.id}" ${x.id===st.selectedGroupId?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div class="right">${contentSection==='scores'?`<div class="tabs"><button data-mode="table" class="${viewMode==='table'?'active':''}">Xem bảng</button><button data-mode="personal" class="${viewMode==='personal'?'active':''}">Xem cá nhân</button></div>`:''}${!admin&&g&&contentSection==='scores'?`<button class="btn primary" id="submit-grading">Khóa & Nộp</button>`:''}</div></div>${contentTabs}<div id="grading-body"></div>`;$('#grade-group').onchange=e=>{st.selectedGroupId=e.target.value;st.selectedCandidateId=null;contentSection='scores';store.emit('selection')};view.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{viewMode=b.dataset.mode;localStorage.setItem('gs28GradingView',viewMode);renderGrading()});view.querySelectorAll('[data-section]').forEach(b=>b.onclick=()=>{contentSection=b.dataset.section;renderGrading()});if($('#submit-grading'))$('#submit-grading').onclick=()=>submitGroup(g);renderBody(g,admin)}
function renderBody(g,admin){const st=store.get(),host=$('#grading-body');if(!g){host.innerHTML='<div class="card pad muted">Chưa có nhóm được phân quyền.</div>';return}if(contentSection!=='scores'){renderContentSection(g,contentSection,host);return}const status=groupStatus(g);if(st.system.emergencyLock){host.innerHTML=`<div class="notice danger">Hệ thống đang khóa khẩn cấp. ${esc(st.system.reason||'')}</div>`;return}if(!admin&&!status.open){host.innerHTML=`<div class="card pad"><h3>${esc(g.name)}</h3><div class="notice warn">${esc(status.label)}. Nhóm vẫn hiển thị vì bạn đã được phân quyền.</div></div>`;return}const cands=groupCandidates(st,g.id);if(!cands.length){host.innerHTML='<div class="card pad muted">Nhóm chưa có thí sinh hoặc dữ liệu chưa mở quyền đọc.</div>';return}if(!st.selectedCandidateId||!cands.some(c=>c.id===st.selectedCandidateId))st.selectedCandidateId=(cands.find(c=>c.queueStatus==='grading')||cands[0]).id;if(viewMode==='table')renderTable(g,cands,admin);else renderPersonal(g,cands,admin)}
function renderContentSection(g,type,host){const key=type==='scoring'?'scoringContent':'interviewContent',access=sectionAccess(g,key),cfg=normalizeContent(g),sec=cfg[key];if(!access.enabled){host.innerHTML='<div class="card pad muted">Admin chưa bật phần này.</div>';return}if(!access.open){host.innerHTML=`<div class="card pad"><div class="notice warn">Nội dung đang khóa: ${esc(access.text)}</div></div>`;return}if(type==='scoring'){const cols=sec.columns||[],rows=sec.rows||[];host.innerHTML=`<div class="card pad"><div class="toolbar"><div><h3 style="margin:0">${esc(sec.title)}</h3><div class="tiny muted">${esc(access.text)}</div></div></div><div class="table-wrap"><table class="table"><thead><tr>${cols.map(c=>`<th>${esc(c.label)}</th>`).join('')}</tr></thead><tbody>${rows.map((r,i)=>`<tr>${cols.map((c,j)=>`<td>${esc(r.cells?.[c.key]??(j===0?i+1:''))}</td>`).join('')}</tr>`).join('')}</tbody></table></div></div>`;return}host.innerHTML=`<div class="card pad"><h3>${esc(sec.title)}</h3><div class="tiny muted">${esc(access.text)}</div>${(sec.groups||[]).map(gr=>`<article style="margin-top:18px"><h4>• ${esc(gr.title)}</h4>${gr.description?`<p class="muted">${esc(gr.description)}</p>`:''}${(gr.questions||[]).map((q,i)=>`<div class="score-box" style="margin-top:8px"><strong>${i+1}. ${esc(q.text)}</strong>${q.note?`<div class="tiny muted" style="margin-top:6px">Gợi ý / lưu ý: ${esc(q.note)}</div>`:''}</div>`).join('')}</article>`).join('')}</div>`}
function renderTable(g,cands,admin){const st=store.get(),host=$('#grading-body');host.innerHTML=`<div class="card"><div class="table-wrap"><table class="table"><thead><tr><th>STT</th><th>SBD</th><th>Thí sinh</th><th>Trạng thái</th>${(g.criteria||[]).map(c=>`<th>${esc(c.name)}<br><span class="tiny">${c.weight}%</span></th>`).join('')}<th>Tổng</th><th>Ghi chú</th></tr></thead><tbody>${cands.map((c,i)=>{const s=userScore(st,c.id),disabled=admin||c.locked||(g.attendanceEnabled&&!c.isPresent);return`<tr><td>${i+1}</td><td>${esc(c.code)}</td><td><strong>${esc(c.name)}</strong>${c.standout?'<span class="badge warn">★ Nổi bật</span>':''}</td><td>${c.locked?'<span class="badge danger">Khóa</span>':(c.isPresent?'<span class="badge ok">Có mặt</span>':'<span class="badge gray">Chờ</span>')}</td>${(g.criteria||[]).map(cr=>`<td><input class="input score-input" style="width:86px" type="number" min="0" max="100" value="${s.scores?.[cr.name]??''}" data-cid="${c.id}" data-crit="${esc(cr.name)}" ${disabled?'disabled':''}></td>`).join('')}<td><strong data-total="${c.id}">${s.totalScore??0}</strong></td><td>${esc(s.candidateComment||'')}</td></tr>`}).join('')}</tbody></table></div></div>`;host.querySelectorAll('.score-input').forEach(inp=>inp.oninput=()=>scheduleSave(g,inp.dataset.cid,host))}
function renderPersonal(g,cands,admin){const st=store.get(),host=$('#grading-body'),c=cands.find(x=>x.id===st.selectedCandidateId)||cands[0],s=userScore(st,c.id),disabled=admin||c.locked||(g.attendanceEnabled&&!c.isPresent);host.innerHTML=`<div class="split"><div class="card candidate-list">${cands.map(x=>{const sx=userScore(st,x.id);return`<div class="candidate-item ${x.id===c.id?'active':''}" data-cand="${x.id}"><div class="candidate-name">${esc(x.name)}</div><div class="tiny muted">${esc(x.code)} · ${x.queueStatus||'waiting'} · ${sx.totalScore??0}đ</div></div>`}).join('')}</div><div class="card pad"><div class="toolbar"><div><div class="tiny muted">THÍ SINH</div><h2 style="margin:3px 0">${esc(c.name)}</h2><div class="muted">${esc(c.code)}</div></div><div class="right">${c.locked?'<span class="badge danger">Đã khóa</span>':''}${c.standout?'<span class="badge warn">★ Nổi bật</span>':''}<span class="badge ${c.isPresent?'ok':'gray'}">${c.isPresent?'Có mặt':'Chưa điểm danh'}</span></div></div><div class="grid grid-4"><div class="stat card"><div class="label">Điểm tổng</div><div class="value" data-total="${c.id}">${s.totalScore??0}</div></div><div class="stat card"><div class="label">Pass</div><div class="value">${g.passScore??0}</div></div><div class="stat card"><div class="label">Kết quả</div><div class="value" style="font-size:18px">${s.candidateId?(Number(s.totalScore)>=Number(g.passScore)?'ĐẠT':'TRƯỢT'):'CHỜ'}</div></div><div class="stat card"><div class="label">Hàng đợi</div><div class="value" style="font-size:18px">${esc(c.queueStatus||'waiting')}</div></div></div>${disabled?`<div class="notice warn" style="margin-top:14px">${admin?'Admin đang xem chế độ chỉ đọc.':c.locked?'Thí sinh đã bị Admin khóa.':'Thí sinh chưa được điểm danh.'}</div>`:''}<h3>Chấm điểm</h3><div class="score-grid">${(g.criteria||[]).map(cr=>`<div class="score-box"><h4>${esc(cr.name)} <span class="tiny muted">${cr.weight}%</span></h4><input class="input score-input" type="number" min="0" max="100" data-cid="${c.id}" data-crit="${esc(cr.name)}" value="${s.scores?.[cr.name]??''}" ${disabled?'disabled':''}><label class="label" style="margin-top:8px">Nhận xét tiêu chí</label><textarea class="textarea criterion-comment" data-crit="${esc(cr.name)}" ${disabled?'disabled':''}>${esc(s.criterionComments?.[cr.name]||'')}</textarea></div>`).join('')}</div><label class="label" style="margin-top:16px">Nhận xét chung về thí sinh</label><textarea id="candidate-comment" class="textarea" ${disabled?'disabled':''}>${esc(s.candidateComment||'')}</textarea>${!admin?`<label style="display:flex;gap:8px;align-items:center;margin-top:12px"><input id="standout" type="checkbox" ${s.standout?'checked':''} ${disabled?'disabled':''}> Đánh dấu thí sinh nổi bật</label>`:''}</div></div>`;host.querySelectorAll('[data-cand]').forEach(x=>x.onclick=()=>{st.selectedCandidateId=x.dataset.cand;store.emit('selection')});host.querySelectorAll('.score-input,.criterion-comment,#candidate-comment,#standout').forEach(inp=>inp.oninput=()=>scheduleSave(g,c.id,host))}
function gatherDraft(g,cid,host){const st=store.get(),current=userScore(st,cid),scores={...(current.scores||{})},criterionComments={...(current.criterionComments||{})};host.querySelectorAll(`.score-input[data-cid="${cid}"]`).forEach(i=>scores[i.dataset.crit]=i.value===''?'':Number(i.value));host.querySelectorAll('.criterion-comment').forEach(i=>criterionComments[i.dataset.crit]=i.value);return{scores,criterionComments,candidateComment:host.querySelector('#candidate-comment')?.value??current.candidateComment??'',standout:host.querySelector('#standout')?.checked??current.standout??false}}
function scheduleSave(g,cid,host){
  const draft=gatherDraft(g,cid,host);pendingDrafts.set(cid,{g,draft,host});clearTimeout(timers.get(cid));timers.set(cid,setTimeout(()=>flushDraft(cid),450));
}
async function flushDraft(cid){
  const item=pendingDrafts.get(cid);if(!item)return;pendingDrafts.delete(cid);clearTimeout(timers.get(cid));timers.delete(cid);
  const {g,draft,host}=item,st=store.get(),c=st.candidates.get(cid);if(!c)return;
  try{const baseRevision=Number(userScore(st,cid).revision||0);const r=await saveScore({grader:st.user,candidate:c,group:g,...draft,baseRevision});const scoreId=`${st.user.authUid}_${cid}`;st.scores.set(scoreId,{id:scoreId,candidateId:cid,groupId:g.id,graderUid:st.user.authUid,graderCode:st.user.code,graderName:st.user.name,...draft,totalScore:r.totalScore,revision:r.revision??baseRevision,offlinePending:!!r.queued});store.emit('scores');if(r.queued)toast('Mất mạng: điểm đã đưa vào hàng chờ đồng bộ','error');else{const el=host?.querySelector?.(`[data-total="${cid}"]`);if(el)el.textContent=r.totalScore;}}
  catch(e){toast(e.code==='SCORE_CONFLICT'?'Điểm vừa được thay đổi ở phiên khác. Tải lại trước khi sửa tiếp.':e.message,'error')}
}
async function submitGroup(g){
  if(!navigator.onLine)return toast('Không thể nộp khi đang offline. Hãy kết nối mạng và chờ đồng bộ điểm.','error');
  for(const [cid,item] of [...pendingDrafts])if(item.g.id===g.id)await flushDraft(cid);
  const {pendingScores}=await import('../offline/scoreQueue.js');if(pendingScores().some(x=>x.groupId===g.id))return toast('Vẫn còn điểm offline chưa đồng bộ. Chưa thể nộp.','error');
const st=store.get(),cands=groupCandidates(st,g.id),problems=validateBeforeSubmit(g,cands,st.scores,st.user.authUid);if(problems.length){const preview=problems.slice(0,5).map(x=>`${x.candidate.name}: thiếu ${x.missing.join(', ')}`).join('\n');return toast(`Chưa thể nộp. ${problems.length} thí sinh thiếu điểm. ${preview}`,'error')}confirmModal('Sau khi nộp, bảng điểm của bạn sẽ bị khóa. Tiếp tục?',async()=>{await setDoc(ref('grader_verifications',`${st.user.authUid}_${g.id}`),{graderUid:st.user.authUid,graderCode:st.user.code,graderName:st.user.name,groupId:g.id,isVerified:true,submittedAt:serverTimestamp()},{merge:true});await audit('GRADING_SUBMITTED',{groupId:g.id,groupName:g.name},st.user);toast('Đã khóa và nộp bảng điểm')})}
