import { store } from '../state/store.js';
import { ref,setDoc,deleteDoc,serverTimestamp,col,getDocs,query,where,chunkedCommit } from '../data/firestore.js';
import { esc,toast,modal,confirmModal,$ } from '../utils/dom.js';
import { audit } from '../services/audit.js';
import { openContentEditor } from './groupContent.js';
export function renderGroups(user){const view=$('#view'),groups=[...store.get().groups.values()];view.innerHTML=`<div class="toolbar"><div><h3 style="margin:0">Quản lý nhóm thi</h3><div class="tiny muted">Mở thủ công/tự động, khóa khẩn cấp từng thí sinh, tiêu chí linh hoạt.</div></div><button class="btn primary" id="add-group">+ Tạo nhóm</button></div><div class="grid">${groups.map(g=>groupCard(g)).join('')||'<div class="card pad muted">Chưa có nhóm.</div>'}</div>`;$('#add-group').onclick=()=>editGroup(null,user);view.querySelectorAll('[data-content]').forEach(b=>b.onclick=()=>openContentEditor(store.get().groups.get(b.dataset.content)));view.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>editGroup(store.get().groups.get(b.dataset.edit),user));view.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=async()=>{const g=store.get().groups.get(b.dataset.toggle);await setDoc(ref('competition_groups',g.id),{isManuallyOpened:!g.isManuallyOpened,updatedAt:serverTimestamp()},{merge:true});toast(!g.isManuallyOpened?'Đã mở nhóm':'Đã khóa nhóm');});view.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>deleteGroupCascade(b.dataset.delete,user))}
function groupCard(g){return `<div class="card pad"><div class="toolbar"><div><h3 style="margin:0">${esc(g.name)}</h3><div class="tiny muted">${g.openMode==='auto'?'Tự động':'Thủ công'} · Pass ${g.passScore??0} · ${(g.criteria||[]).length} tiêu chí</div></div><div class="right"><span class="badge ${(g.isManuallyOpened||g.openMode==='auto')?'ok':'warn'}">${g.openMode==='manual'?(g.isManuallyOpened?'Đang mở':'Chưa mở'):'Theo giờ'}</span><button class="btn small" data-content="${g.id}">Nội dung chấm</button><button class="btn small" data-edit="${g.id}">Chỉnh sửa</button>${g.openMode==='manual'?`<button class="btn small ${g.isManuallyOpened?'danger':'ok'}" data-toggle="${g.id}">${g.isManuallyOpened?'Khóa':'Mở'}</button>`:''}<button class="btn small danger" data-delete="${g.id}">Xóa</button></div></div></div>`}
function editGroup(g,user){const criteria=g?.criteria?.length?g.criteria:[{name:'Tiêu chí 1',weight:100}];const rows=criteria.map((c,i)=>`<div class="grid grid-2 crit-row"><input class="input crit-name" value="${esc(c.name)}" placeholder="Tên tiêu chí"><input class="input crit-weight" type="number" value="${Number(c.weight||0)}" placeholder="Trọng số %"></div>`).join('');const root=modal({title:g?'Chỉnh sửa nhóm':'Tạo nhóm',body:`<div class="grid grid-2"><div><label class="label">Tên nhóm</label><input id="g-name" class="input" value="${esc(g?.name||'')}"></div><div><label class="label">Điểm Pass</label><input id="g-pass" class="input" type="number" value="${Number(g?.passScore||50)}"></div><div><label class="label">Chế độ mở</label><select id="g-mode" class="select"><option value="manual" ${g?.openMode!=='auto'?'selected':''}>Thủ công</option><option value="auto" ${g?.openMode==='auto'?'selected':''}>Tự động</option></select></div><div><label class="label">Yêu cầu điểm danh</label><select id="g-att" class="select"><option value="1" ${g?.attendanceEnabled?'selected':''}>Có</option><option value="0" ${!g?.attendanceEnabled?'selected':''}>Không</option></select></div><div><label class="label">Giờ mở</label><input id="g-start" class="input" type="datetime-local" value="${toLocal(g?.startTime)}"></div><div><label class="label">Giờ khóa</label><input id="g-end" class="input" type="datetime-local" value="${toLocal(g?.endTime)}"></div></div><h4>Tiêu chí</h4><div id="criteria-list" class="grid">${rows}</div><button class="btn small" id="add-crit" style="margin-top:10px">+ Tiêu chí</button>`,actions:`<span></span><div><button class="btn" data-close>Hủy</button> <button class="btn primary" id="save-group">Lưu</button></div>`});root.querySelector('#add-crit').onclick=()=>{const d=document.createElement('div');d.className='grid grid-2 crit-row';d.innerHTML='<input class="input crit-name" placeholder="Tên tiêu chí"><input class="input crit-weight" type="number" value="0" placeholder="Trọng số %">';root.querySelector('#criteria-list').appendChild(d)};root.querySelector('#save-group').onclick=async()=>{const name=root.querySelector('#g-name').value.trim();if(!name)return toast('Tên nhóm không được trống','error');const criteria=[...root.querySelectorAll('.crit-row')].map(r=>({name:r.querySelector('.crit-name').value.trim(),weight:Number(r.querySelector('.crit-weight').value||0)})).filter(x=>x.name);const sum=criteria.reduce((s,x)=>s+x.weight,0);if(Math.abs(sum-100)>0.01)return toast(`Tổng trọng số phải bằng 100% (hiện ${sum}%)`,'error');const id=g?.id||crypto.randomUUID();await setDoc(ref('competition_groups',id),{name,passScore:Number(root.querySelector('#g-pass').value||0),criteria,openMode:root.querySelector('#g-mode').value,attendanceEnabled:root.querySelector('#g-att').value==='1',startTime:root.querySelector('#g-start').value?new Date(root.querySelector('#g-start').value):null,endTime:root.querySelector('#g-end').value?new Date(root.querySelector('#g-end').value):null,isManuallyOpened:g?.isManuallyOpened||false,updatedAt:serverTimestamp(),createdAt:g?.createdAt||serverTimestamp()},{merge:true});await audit(g?'GROUP_UPDATE':'GROUP_CREATE',{groupId:id,groupName:name},user);root.innerHTML='';toast('Đã lưu nhóm')}}
function toLocal(v){if(!v)return'';const d=v.toDate?v.toDate():new Date(v);if(Number.isNaN(d.getTime()))return'';d.setMinutes(d.getMinutes()-d.getTimezoneOffset());return d.toISOString().slice(0,16)}
async function deleteGroupCascade(id,user){confirmModal('Xóa nhóm sẽ xóa toàn bộ thí sinh, điểm, lịch sử, trạng thái nộp và gỡ phân quyền nhóm. Tiếp tục?',async()=>{try{const collections=['candidates','scores','audit_logs','grader_verifications','submission_notifications'];const ops=[];for(const c of collections){const s=await getDocs(query(col(c),where('groupId','==',id)));s.forEach(d=>ops.push({type:'delete',ref:d.ref}))}ops.push({type:'delete',ref:ref('competition_groups',id)});ops.push({type:'delete',ref:ref('attendance_realtime',id)});const users=await getDocs(col('users'));users.forEach(d=>{const x=d.data();if((x.assignedGroups||[]).includes(id))ops.push({type:'set',ref:d.ref,data:{assignedGroups:(x.assignedGroups||[]).filter(v=>v!==id)},options:{merge:true}})});await chunkedCommit(ops);await audit('GROUP_DELETE',{groupId:id},user);toast('Đã xóa nhóm và dữ liệu liên quan')}catch(e){toast(e.message,'error')}})}
