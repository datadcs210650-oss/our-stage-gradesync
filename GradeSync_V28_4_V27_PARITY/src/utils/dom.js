export const $=(s,r=document)=>r.querySelector(s);export const $$=(s,r=document)=>[...r.querySelectorAll(s)];
export function esc(v=''){return String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;')}
export function decodeEntities(v=''){let s=String(v??'');for(let i=0;i<4;i++){const t=document.createElement('textarea');t.innerHTML=s;const n=t.value;if(n===s)break;s=n}return s}
export function toast(message,type='ok'){const root=$('#toast-root');const el=document.createElement('div');el.className=`toast ${type}`;el.textContent=String(message);root.appendChild(el);setTimeout(()=>el.remove(),4200)}
export function modal({title,body,actions=''}){const root=$('#modal-root');root.innerHTML=`<div class="modal-backdrop"><div class="modal"><div class="modal-head"><strong>${esc(title)}</strong><button class="btn small" data-close>Đóng</button></div><div class="modal-body">${body}</div>${actions?`<div class="modal-foot">${actions}</div>`:''}</div></div>`;root.querySelectorAll('[data-close]').forEach(x=>x.onclick=()=>root.innerHTML='');return root}
export function confirmModal(message,onYes){const root=modal({title:'Xác nhận',body:`<p>${esc(message)}</p>`,actions:`<span></span><div><button class="btn" data-close>Hủy</button> <button class="btn danger" id="confirm-yes">Đồng ý</button></div>`});root.querySelector('#confirm-yes').onclick=async()=>{root.innerHTML='';await onYes()}}
export function downloadBlob(blob,name){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
export function fmtTime(ts){if(!ts)return'-';const d=ts.toDate?ts.toDate():new Date(ts);return Number.isNaN(d.getTime())?'-':d.toLocaleString('vi-VN')}
